.worktrees/
node_modules/
dist/
coverage/
.env
.env.*
!.env.example

# Local agent planning and review artifacts
.superpowers/
docs/superpowers/

# Multi-process test scratch dirs. They sit in the repo root because the worker
# each test writes imports ../src, and a run killed mid-test skips its cleanup.
.test-scratch-*/
